import { Typography } from "@arco-design/web-react";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return (
    <div className="p-6">
      <Typography.Title heading={2}>设置</Typography.Title>
      <Typography.Paragraph>设置页面施工中…</Typography.Paragraph>
    </div>
  );
}
